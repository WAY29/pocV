name: poc-yaml-xray-group-test-example-com
groups:
  example1:
    - method: GET
      path: "/"
      expression: |
        response.status==200 && response.body.bcontains(b'Example Domain')
  example2:
      - method: GET
        path: "/"
        expression: |
          response.status==200 && response.body.bcontains(b'Example1 Domain')


detail:
  author: test(http://example.com)
  links: 
    - http://example.com
  tags: test